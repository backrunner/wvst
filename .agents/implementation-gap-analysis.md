# WVST Implementation Gap Analysis

更新日期：2026-06-03

## 当前已完成能力

- Rust workspace、TS/Rollup Web SDK、协议帧和基础测试已建立。
- Bridge Server 已提供 localhost WebSocket、hello/version negotiation、origin/token 校验、metrics 和 WVST 二进制音频帧路由/诊断。
- Web SDK 已提供 `WVSTClient.connect()`、低延迟前置条件检查、插件 scan/list API、worker/worklet loopback 基础模块。
- macOS VST3 scanner 已支持默认路径和自定义路径，能解析 `moduleinfo.json` 并回退到 bundle 名称。
- `wvst-host-worker` 已作为独立进程入口，支持 VST3 bundle 描述、module symbol probe、macOS factory info 读取、component probe、runtime probe 和 framed worker 服务。
- Bridge 已通过 `plugin.factoryInfo` 将 factory metadata 请求路由到隔离 worker，Bridge 自身不加载第三方 VST。
- Bridge 控制面已增加连接级会话门禁，除 `bridge.hello` 外的 API 需要先完成授权 hello。
- Bridge/Web SDK 已提供 `instance.create` / `instance.list` / `instance.destroy` 控制面，能为已扫描插件分配独立 `instanceId` 与 `streamId`。
- `wvst-host-worker serve-framed` 已提供常驻 framed control IPC，使用 `WVCI` 固定 header + JSON body 承载 worker hello、instance lifecycle 和参数/状态控制请求；旧 `serve` JSON-line 控制模式已删除。音频处理已从 debug JSON 小块处理迁移到独立二进制 audio IPC。
- Bridge `instance.create` 已能默认启动并绑定 `wvst-host-worker serve-framed`，通过 `framedControlIpc` capability 校验后进入 `ready` / `ready` 状态。
- Bridge worker supervisor 已加入 stderr 摘要、启动失败计数和基础 quarantine，避免同一故障插件无限重启。
- Bridge worker supervisor 已加入应用级 worker 实例数量上限，默认最多 64 个活跃 worker，可通过 `WVST_MAX_WORKER_INSTANCES` 或 runtime builder 配置；超限时控制面会先尝试保守回收同插件、stream 已关闭、ready/stopped 的 idle worker 实例，并通过 `worker-policy-decision` 暴露 `resource-limit/reclaim/idle-worker` 决策；没有安全候选时返回结构化 `resource-limit-exceeded` 错误并清理未启动实例记录。
- Bridge/Web SDK 已提供 `instance.status` heartbeat API，能通过 worker `worker.metrics` 检查实例 worker 存活，并在 worker 退出或 IPC 断开时把实例标记为 `failed`。
- Bridge/Web SDK 已提供 `instance.restart` 手动恢复 API，能在保留 `instanceId` / `streamId` 的情况下杀掉旧 worker 并重新拉起同一实例；Bridge metrics 已暴露 `workerFailures` 和 `workerRestarts`。
- Bridge `instance.status` 已加入可配置的 worker 自动恢复策略：heartbeat 失败后默认尝试重启同一实例并保留 `instanceId` / `streamId`，如果实例原先处于 `processing` 会重新进入 processing；Bridge metrics 已暴露 `workerAutoRestarts`。
- Bridge worker supervisor 已接入 kill/wait shutdown audit metrics，`bridge.metrics` / Web SDK 可观测 `workerShutdowns`、`workerKillRequests`、`workerTreeKillRequests`、`workerForcedKillRequests`、`workerWaitSuccesses` 和 `workerWaitTimeouts`。
- Bridge/Web SDK 已提供 `instance.start` / `instance.stop` 处理生命周期控制，实例状态可从 `ready` 切到 `processing` / `stopped`，并已补入 `starting` / `stopping` / `recovering` 瞬态状态。
- Bridge 已提供运行时事件总线、`bridge.events` 控制面查询和授权后 WebSocket `bridge.event` server-push notification；Web SDK 已暴露 `client.events()` 轮询和 `client.onEvent()` 主动订阅，可观察 server lifecycle、worker start/ready/processing/stopped/failed/recovering/recovered/quarantine 事件；`worker-failed` 事件会携带可选 `errorData`，数据面 audio process 失败也会发布包含 worker/runtime 结构化原因的失败事件。
- Bridge worker supervisor 已提供 quarantine TTL 释放策略，过期释放会清空累计失败计数并可通过事件观测；quarantine error data 与 `worker-quarantined` event 已暴露 `releaseAfterMs`，便于 Web/UI 展示重试倒计时和策略诊断；`worker-recovering` / `worker-recovered` / `worker-recovery-failed` event 已通过 `mode`、`reason` 和结构化 `errorData` 区分手动 restart、heartbeat 自动恢复、重启失败与 processing 恢复失败。
- Bridge worker supervisor 的 quarantine failure threshold 已可通过 `WVST_WORKER_QUARANTINE_FAILURES`、`BridgeConfig::with_worker_quarantine_failure_threshold()` 和 `BridgeRuntimeBuilder::worker_quarantine_failure_threshold()` 配置；嵌入式宿主可按自身恢复策略调节插件进入 quarantine 前允许的失败次数。
- Bridge worker supervisor 已提供首版 worker 进程树终止：Unix/macOS 启动 worker 时放入独立 process group，shutdown 时优先向 process group 发终止信号并等待，超时后升级强制 kill；测试覆盖 worker 派生子进程后 destroy 仍能清理进程组。
- Bridge worker supervisor 生产路径已默认通过 `WVCI` framed control IPC 发送/接收 worker 控制请求；worker hello 会暴露并校验 `framedControlIpcVersion`、`framedControlMaxBodyBytes`、sequence id、status code 和 error response frame capability，并暴露 `framedControlBatching`。测试覆盖 framed worker create/destroy、真实 worker 缺失 VST3 bundle 拒绝、版本不匹配和 body cap/capability 不匹配拒绝路径。framed control IPC 已能在 header 层区分 `Response` / `ErrorResponse`，worker JSON-RPC error 会映射到非零 `statusCode`，Bridge 会校验 frame kind、sequence 和 status 后再解析 body；协议、worker serve-framed 与 Bridge supervisor 已支持 `BatchRequest` / `BatchResponse`，可在一个外层 frame 内承载多个独立 sequence 的控制请求/响应。
- Bridge 音频路由现在要求实例处于 `processing` 状态；未 start、已 stop 或处理失败都会返回带 `silence` / `process-error` 的诊断静音帧，而不是继续把音频送进 worker。
- Instance heartbeat 已避免把正在 `processing` 的实例误降回 `ready`，降低控制面状态刷新对数据面的干扰。
- Bridge worker supervisor 已校验 `worker.hello` 中的 `ipcVersion`、`instanceLifecycle` 和 `binaryAudioProcess` capability，避免 Bridge 与不兼容 worker 继续创建实例。
- Bridge 创建 worker 实例时已把 `sampleRate` 和 `maxBlockFrames` 传给 worker；worker audio IPC 会验证 sample rate、最大 block、输入通道数和 processing 状态。
- `wvst-host-worker` 已为每个 VST3 runtime 实例预分配 audio scratch buffers，并复用 audio IPC request/response body buffers；worker audio IPC 不再为每个 block 重复分配输入/输出 sample Vec 或响应 frame Vec。
- Bridge 二进制音频帧已能按 `streamId` 路由到对应 worker 的独立二进制 audio IPC，并回传 worker 处理后的 F32 frame；合法 WVST audio frame 若 stream 未匹配或长度异常会返回 `silence` / `process-error` 诊断帧并计入 route failure，非 WVST 二进制会作为协议错误返回空 binary 并计入 route failure。
- Bridge metrics 已区分二进制帧总量、成功路由音频帧、坏 header、音频路由失败、WVST frame 长度异常和 stream 未匹配；非 WVST binary 不再 echo，而是作为协议错误返回空 binary 并计入 route failure，便于后续接入 drop/late/underflow/overflow 统计与生命周期错配诊断。
- Bridge metrics 已加入二进制音频路由耗时直方图和 `audioRouteLatency` p50/p95/p99 微秒级快照，Web SDK metrics 类型已同步。
- Bridge 已加入按 stream 的音频序号诊断 tracker，能统计 sequence gap 事件/缺失帧估算、重复帧、乱序帧、late flag 和 interarrival jitter 分位数；stream close/open/destroy 会重置 tracker，避免 Web 端重开流后的误报。
- Bridge 音频路由已加入每 stream in-flight limiter：同一 stream 的上一块音频仍在 worker 处理时，新 block 会被判定为 backpressure drop，并返回带 `silence` / `late` flag 的诊断静音帧；Bridge/Web metrics 已暴露 `audioBackpressureDrops`。
- Bridge/Web SDK 已提供 `stream.open` / `stream.close` 控制 API，实例记录包含 `streamState`，Bridge 只将 open stream 的音频帧路由到 worker。
- 对于已知 stream 的关闭或处理失败场景，Bridge 会返回带 `silence` / `end-of-stream` / `process-error` flag 的诊断静音音频帧，避免把异常伪装成正常 echo；`stream.close` 控制面会等待同 stream 的在途音频块释放或超时后再返回，避免 close 与最后一个 block 竞争；Bridge event bus 已发布 `stream-opened`、`stream-closing` 和带 `drainTimedOut` 的 `stream-closed` 事件。
- Web `bridge-worker` 已具备从 SAB input ring 读取 quantum、编码 WVST binary audio frame、发送 Bridge 并写回 output ring 的基础 audio pump；AudioWorklet processor 已支持通过 SAB ring 和计数器交换音频块；Web worker 会在输入 ring 追上实时端时主动跳到最新 quantum、统计实际丢弃的 input/output quantum，并在 Bridge binary 响应损坏或 transport 失败时写入诊断静音帧保持 WebAudio 时钟推进。
- Web SDK 已提供 `WVSTBridgeWorkerClient`，封装 bridge worker 的 connect/request/sendBinary/startAudioStream/stopAudioStream 命令，避免应用侧手写 worker message protocol。
- Web SDK 已提供音频设备选择 helper：可枚举 `audioinput`/`audiooutput`，按 `deviceId` 请求输入 `MediaStream`，创建 `MediaStreamAudioSourceNode`，并通过 `AudioContext.setSinkId()` 或 `MediaStreamAudioDestinationNode + HTMLMediaElement.setSinkId()` 指定输出设备。
- Web SDK 已提供 `createWVSTAudioDeviceSession()` 高层 session graph helper，可按 Web 指定的输入/输出设备把 `MediaStreamAudioSourceNode`、WVST AudioWorklet/SAB、Bridge worker audio pump 和 media-element output route 串起来，并支持 0-input 音源 VST 的 Web 数据面启动。
- Web SDK 已提供音频设备 capability API、`devicechange` watcher，以及 session 运行期 `setInputDevice()` / `setOutputDevice()` 切换方法。
- Web session 已校验 `AudioContext.sampleRate` 与 instance `sampleRate` 一致，提供 `restartAudioStream()` 重启 Bridge worker audio pump，并在主线程 session monitor 中按 `transportFailures` 增量自动串行重启数据泵；`autoRestartAudioStream` 可配置监测间隔、重启限频、最大尝试次数和 restart/failure/rebuild callbacks。`getMetrics()` 已暴露 loopback underflow/overflow、pending quantum、dropped input/output quantum、late/dropped MIDI/parameter event、transport failure、audio stream restart/failure、auto-restart exhausted、sample-rate mismatch 和 `rebuildRequired` 指标。
- `wvst-protocol` 和 Web SDK 已定义固定 16 字节 MIDI/note event schema，事件包含 sample offset、kind、channel、data bytes 和 note id；audio frame payload 已能表达 audio samples 后追加 event section。
- Web SDK 已提供 `sendMidiEvents()` 路径，DedicatedWorker 会缓存 MIDI events、绑定目标 WebAudio quantum、按 sample offset 排序，并随对应 audio frame 发送到 Bridge；如果 pump 已越过目标 quantum，事件会作为 late event 丢弃并计数，避免把过期 MIDI 注入错误 block；Bridge/worker 音频 IPC 已能接受 event section，worker 会将 note on/off、poly pressure 和对应 raw MIDI note 事件转换为 VST3 `IEventList` 输入。
- Web SDK 已提供 `createWVSTWebMidiAdapter()`、`webMidiMessageToWVSTEvents()`、`createWVSTVirtualKeyboard()` 和默认 QWERTY keymap，可把 Web MIDI channel voice message、raw fallback、虚拟 note/control/pitch/aftertouch 和键盘事件映射为 WVST `MidiEvent`，再通过 session `sendMidiEvents()` 进入现有数据面；事件 sample offset 默认投到下一块起点，也可由调用方显式指定。
- `wvst-protocol` 和 Web SDK 已新增固定 16 字节 parameter automation event schema，audio frame header 使用保留字段携带 `parameterEventCount`，payload 现在可表达 audio samples + MIDI events + parameter events。
- worker 音频 IPC 边界已对输入 MIDI/note events、parameter automation、MIDI mapping 产生的参数变化，以及插件输出回 Web 的 MIDI/parameter events 按 block 内 sample offset 做确定性排序；同 sample offset 的 MIDI/note events 保留原始相对顺序，parameter changes 再按 ParamID 稳定归并。
- Rust audio frame 协议已允许 `channels = 0`，VST3 worker 路径已支持 zero-input instrument frame 并覆盖测试。
- `wvst-vst3-host` 已增加 VST3 FUID 规范化、`IPluginFactory::createInstance` ABI skeleton 和 macOS `create_vst3_component_probe()` safe facade；`createInstance` 现已按 VST3 ABI 传递 16-byte TUID class/interface id，而不是 registry hex 字符串；`wvst-host-worker component-probe <plugin.vst3> <class-id>` 可在隔离 worker 内验证 component 创建并释放。
- VST3 ABI 边界已补入 `IPluginBase`、`IComponent`、`IAudioProcessor`、`IHostApplication`、`ProcessSetup`、`AudioBusBuffers` 和 `ProcessData` 的 Rust repr(C) skeleton，`queryInterface` 已改为传递 16-byte TUID 而非 FUID 字符串，后续真实 process path 可以继续在 `wvst-vst3-host` 内收敛 unsafe。
- `create_vst3_component_probe()` 现在会通过 `queryInterface` 验证 component 是否暴露 `IAudioProcessor`；`wvst-host-worker component-probe` 可继续作为隔离探测命令使用。
- `wvst-vst3-host` 已加入纯 Rust `Vst3Lifecycle` 状态机和 `Vst3ProcessingConfig`，覆盖 `created -> initialized -> setup-done -> activated -> processing -> stopped -> terminated` 的合法顺序和非法转移测试。
- `wvst-vst3-host` 已加入 `Vst3ProcessBuffers`，能预分配 planar input/output buffer，将 interleaved f32 输入转换为 VST3 channel buffers，并把 planar 输出复制回 interleaved f32；同时覆盖无输入音源 VST 的 buffer 路径。
- `wvst-vst3-host` 已加入 VST3 `IEventList` / `Event` ABI skeleton 和 `Vst3EventList` safe wrapper，`ProcessData.input_events` / `output_events` 现在都指向稳定的预分配事件列表，每个 block 会清空旧事件；输入侧会填充新的 note/poly pressure 事件，输出侧可捕获插件通过 `addEvent` 写回的事件。
- `wvst-vst3-host` 已加入 VST3 `IParameterChanges` / `IParamValueQueue` ABI skeleton 和 `Vst3ParameterChanges` safe wrapper，`ProcessData.input_parameter_changes` / `output_parameter_changes` 现在都指向稳定的 host-owned 参数队列；输入侧会按 ParamID 分组填充 sample-accurate normalized automation points，输出侧可捕获插件通过 `addParameterData/addPoint` 写回的参数变化。
- `wvst-vst3-host` 已加入 VST3 host-owned `ProcessContext` 和 `IProcessContextRequirements` 查询，`ProcessData.process_context` 指向稳定堆内存并随 block 推进 sample timeline、tempo、拍号和 musical position；worker diagnostics 已暴露插件声明的 process context requirements bitmask。
- `wvst-vst3-host` 已加入 `Vst3AudioProcessor` facade，能封装 owned `IAudioProcessor` 指针并调用 `canProcessSampleSize`、`setupProcessing`、`setProcessing`、`process`、latency/tail 查询；fake ABI fixture 已覆盖真实 `ProcessData` 指针链路。
- `wvst-vst3-host` 已加入 `Vst3ComponentInstance` holder，能持有 owned `IComponent` + `Vst3AudioProcessor`，并将 initialize、setupProcessing、setActive、setProcessing、process、terminate 串入 `Vst3Lifecycle`；fake component/processor fixture 已覆盖完整生命周期和错误传播。
- `wvst-host-worker runtime-probe <plugin.vst3> <class-id>` 已提供隔离进程内真实 VST3 runtime smoke 入口，可按指定 sample rate、block size、输入/输出通道、frames、blocks、MIDI note 和参数自动化输入创建 component/controller、完成 setup/activate/start/process/stop/terminate，并输出参数数量、controller 参数 flag 计数、controller 初始化后 component-state 同步到 controller 的 attempted/success/bytes/error 诊断、component/controller state 读取摘要与默认主动 state roundtrip 成功状态（可用 `--skip-state-roundtrip-probe` 跳过）、component handler 安装、默认主动 edit-probe 成功状态与事件增量（可用 `--skip-controller-edit-probe` 跳过）、component/controller connection-point 连接状态与默认主动 notify-probe 成功状态（可用 `--skip-connection-notify-probe` 跳过）、unit/program metadata 与 program/unit data 支持摘要、audio bus、latency、raw `tailSamples` 与语义化 `tailInfo.kind`、process context requirements、process timing、output event/parameter diagnostics、最多 16 条 advanced output event 固定字段与有界 payload/text 预览样本、output energy/RMS、silent/non-zero block、first non-zero block/frame/absolute-frame、peak block、non-finite、clipped sample 计数，以及 note-on/note-off 到首个非零输出的 frame/microsecond timing JSON；process 失败时会尽量执行 stop/terminate 清理，便于第三方 effect/instrument 兼容验证自动化。
- `wvst-vst3-host` 已接入基础 audio bus 配置：`setupProcessing` 前调用 `setBusArrangements` 设置 mono/stereo 或 zero-input instrument arrangement，`activate/terminate` 会开关主 audio input/output bus，并覆盖 `setActive` 失败后的 bus rollback。
- `wvst-vst3-host` 已补入 VST3 `BusInfo` ABI 和 `Vst3AudioBusInfo` safe facade，component holder 可查询 audio input/output bus count、channel count、bus type、default active flag 和 UTF-16 bus name。
- component holder 的 audio bus activation 已从固定 index 0 改为基于查询结果选择 bus：优先匹配目标 channel count 的 main bus，其次 default-active main bus，再回退到第一个可用 bus；`wvst-vst3-host` 已提供 selected audio bus safe facade，`wvst-host-worker` runtime diagnostics 与 `runtime-probe` 报告会暴露 input/output 可用 bus、requested channel count、selected index 和 selected bus 详情，便于 Web/UI 和测试矩阵观察真实 VST3 I/O 选择。
- macOS factory runtime 已提供 `create_vst3_component_instance()`，可通过 `IPluginFactory::createInstance(IComponent)` 和 `queryInterface(IAudioProcessor)` 创建 `Vst3LoadedComponent`，并保持 bundle 生命周期覆盖 component/processor holder；`getControllerClassId` 返回 `kResultFalse` / `kNotImplemented` 时会被视作无独立 controller，避免 Reaktor 6 这类真实插件因可选 controller 缺失而中断音频 runtime probe。
- `Vst3ComponentInstance::initialize()` 已传入 WVST `IHostApplication` host context，插件可通过 `queryInterface(IHostApplication)` 读取宿主名称；host-side `createInstance()` 已支持创建 host-owned `IMessage` 和 `IAttributeList` 对象，覆盖 controller/editor communication 常见宿主对象请求。
- `wvst-host-worker serve-framed` 已接入 VST3 runtime backend：instance create 可持久保存 `Vst3LoadedComponent`，`instance.start/stop/destroy` 会驱动真实 VST3 lifecycle，worker audio IPC 可调用真实 `process()`；缺失 class id、invalid class id 或非 bundle 路径都会明确拒绝，不再创建内部 fake runtime。
- worker create response、worker metrics、Bridge instance record 和 Web SDK `InstanceDescriptor` 已暴露 backend、`latencySamples`、`tailSamples` 和 `tailInfo`，Web 侧可以在挂载后读取插件处理延迟，并能区分 no-tail、finite-tail 与 VST3 infinite-tail。
- VST3 controller 基础链路已接入：component 可查询 controller class id，macOS factory runtime 会创建可选 `IEditController`，worker 初始化 controller、注册可记录 begin/perform/end edit、restartComponent、dirty/editor/group-edit 的 `IComponentHandler`/`IComponentHandler2`，controller 初始化后会尝试读取 component state 并调用 controller `setComponentState()` 同步初始 processor state，Web/Bridge 写入 component state 时也会刷新这条 controller 同步诊断，结果进入 worker diagnostics/runtime-probe/matrix summary，并在 component/controller 都支持 `IConnectionPoint` 时建立/释放双向连接；Bridge/worker/Web 控制面已暴露参数列表、unit/program metadata、normalized 参数读写、normalized/plain/display string 转换、component/controller state base64 get/set 聚合、Web 侧实例 state snapshot JSON helper 和目标实例兼容性检查、unit selection、unit-by-bus 查询、`setUnitProgramData`、`IProgramListData` 和 `IUnitData` 数据读写。
- `wvst-vst3-host` 已提供 `IBStream` 内存流、`IComponentHandler`/`IComponentHandler2` host callback 事件快照、`IConnectionPoint` component/controller 通信 facade、`IConnectionPoint::notify(IMessage*)`、host-owned `IMessage` typed attributes helper 和 `Vst3EditController` safe facade，并用 fake ABI 覆盖参数信息、参数设置、state 写入、handler edit/restart/dirty/editor/group-edit callbacks、连接点 connect/disconnect/notify 和生命周期释放。
- `wvst-host-worker` metrics 已暴露 VST3 runtime diagnostics，其中包含 controller `IComponentHandler` 最近事件、累计事件数和 component/controller `connectionPoints.connected` 诊断；Bridge 已在 `instance.status` heartbeat 路径上把新增 handler event 增量转换为 `bridge.event` server-push 事件，并能报告 recent-event ring 溢出导致的 lost sequence；`restartComponent` raw flags 已解码为结构化 `restartFlags`（latency/io/parameter title/value/MIDI mapping 等变化），Bridge 会额外发布 `vst3-metadata-invalidated` 事件指示 Web 侧刷新 parameter values/info、latency、I/O、MIDI mapping 等 metadata，并附带 `refreshPolicy` 区分 metadata refresh、audio graph rebuild 和 component reload；Web SDK 事件和 runtime diagnostics 类型已同步，并提供 `refreshMetadataForInvalidation()` / `onMetadataInvalidated()` helper 自动拉取受影响的 status、parameters、units 和可选 parameter values。
- Bridge 控制面已提供 `instance.metadata.refresh` 批量刷新入口，可通过 framed control batch 在一次 worker 往返中获取参数列表、unit metadata、可选 state 和默认 worker metrics；`instance.runtime.snapshot` 可进一步把实例记录、batch metadata refresh、共享内存 transport / pump 数据面状态和可选 recent events 聚合成一次 UI 挂载快照；`instance.parameter.edit` 可把 UI 单次参数写入的 begin/perform/end edit gesture 聚合为一次 Bridge 请求，`instance.state.setAndRefresh` 可在写入 component/controller state 后同批拉取参数、unit metadata、可选 state 和 worker metrics；`instance.setUnitProgramDataAndRefresh`、`instance.programData.setAndRefresh` 和 `instance.unitData.setAndRefresh` 可在写入 unit/program data 后同批刷新挂载 metadata；`instance.connection.notifyComponentAndRefresh` / `instance.connection.notifyControllerAndRefresh` 可在 component/controller message notify 后同批刷新 metadata；Web SDK 已为这些组合调用和 `instance.runtime.snapshot` 暴露 typed facade，测试 worker fixture 也使用 framed batch，不再保留顺序请求降级。
- `wvst-host-worker` 已为每个实例缓存并暴露带 `schemaVersion` 的 `runtimeCapabilities`，Bridge `InstanceRecord` / Web SDK `InstanceDescriptor` / worker runtime metrics 均可读取当前实例对 binary audio、component/controller state、parameters、parameter automation、unit/program data、MIDI mapping、output events、component handler events、connection points 和 process context 的支持情况；`runtimeCapabilities` schema v2 已新增 `unavailable[]` 结构化诊断，为每个缺失能力暴露 capability、reason、message 和 hint，可区分 controller 不可用、接口缺失、插件未提供功能和 probe 失败；worker JSON-RPC error 已支持结构化 `data`，Bridge 会把 worker rejection data 保留到 `error.data.workerData`，VST3 runtime init 失败可暴露 component create/initialize、controller initialize、setup、activate 阶段、host error kind 和 schema version 1 compatibility category/hint；audio IPC process error 已支持结构化 JSON body，VST3 `process()` 失败可透传 `vst3-runtime-process`、`component.process`、host error kind 和 compatibility category。worker control/audio IPC 构造端已校验 request/response status 必须为 0、error status 必须非 0，并在读入 body 前应用协议级最大 body 长度，避免损坏 header 触发大内存分配。
- VST3 参数、state、unit/program data、connection notify 和 lifecycle 控制面失败已通过 worker JSON-RPC `error.data` 暴露 `vst3-runtime-control`、stage、host error kind 和 message；Bridge 继续通过 `workerData` 原样透传给 Web。VST3 host `IBStream` state/data 导出已加入 8MiB bounded writable stream，Web/worker 输入侧 state、program data、unit data 和 connection message binary/string/attribute count 也已加上显式限制，Bridge WebSocket text 控制消息默认按 framed control IPC 16MiB 上限提前拒绝。
- `wvst-testkit` 已新增纯 Rust latency harness、stability runner、stability budget evaluator、scripted latency snapshot builder、package evidence evaluator 和 runtime probe matrix runner，可记录 sequence gap、duplicate/out-of-order、drop、timeout、late/silence/process-error 计数，并输出 route latency、round-trip frames 与 round-trip microseconds 的 p50/p95/p99 快照；runner 默认按 30 分钟稳定性窗口推进 block/sequence/frame timeline，`wvst-latency-snapshot` CLI 可把 schema version 1 逐块观测 JSON 规范化为预算可消费的 `LatencySnapshot`，budget evaluator 可把最小观测数、drop/timeout/late/silence/process-error、序号异常、route/round-trip p95/p99，WebAudio loopback input/output frames、underflow/overflow、pending input/output quantum、dropped input/output quantum、late/dropped MIDI/parameter event、transport failure、WebAudio end-to-end round-trip p95/p99，以及 Bridge shared-memory pump preflight/overrun/underrun/backpressure/worker-error、pump event queue enqueued/drained/late/dropped/cleared、shared-memory process latency p95/p99 和 pump last/max process/overrun timing 阈值转换为可序列化 pass/fail 报告，`wvst-stability-budget` CLI 可读取 latency snapshot、budget、可选 WebAudio metrics JSON 和可选 Bridge metrics JSON 并用退出码表达是否达标；Bridge metrics 输入已能直接归一化 `bridge.metrics` 原始 JSON-RPC result、`stream.sharedMemory.pump.status` result 和 `instance.runtime.snapshot` 中的 pump status，便于真实数据面压测进入 CI 且避免手工拼接证据；`wvst-package-evidence` CLI 可读取 packager 产出的 `wvst-package-manifest.json` / `wvst-verify-report.json` 和可选 budget，校验 schema/platform/status、failed/skipped/warning counters、strictSignature、平台服务类型、声明的 runtime bridge/host-worker 文件与 role/executable、verify script、manifest/report 检查项一致性、必需 verification checks 和必需 file role/path，并以 JSON report 与退出码表达发布包 evidence 是否达标；runtime matrix runner 可用固定 case 清单调用隔离 worker 的 `runtime-probe` 批量记录 effect/instrument、MIDI note 和参数自动化 smoke 结果，并支持 per-case `timeoutMillis`，worker ANR 会杀掉该 case 并以 `timed-out` 计入报告，避免单个第三方插件拖死整组验证；runtime matrix 已支持 schema version 1 JSON manifest 解析/校验，`.agents/runtime-probe-matrix.example.json` 提供 effect automation 与 zero-input instrument note 示例，manifest 可通过 `fixtureRoot`、`${ENV_VAR}` 和 `~/` 路径展开适配本机/CI 的真实 VST3 fixture 目录，并新增 `wvst-runtime-matrix` CLI 输出 schema version 1 JSON report、通过/失败退出码、顶层 `audioHealth` / `audioBuses` / `noteTiming` / `processOutput` / `processTiming` / `runtimeCharacteristics` / `controllerHealth` 汇总、compatibility/classification 诊断分类汇总、真实插件 evidence coverage 汇总和 case-level expectations；fixture 可声明第三方插件 evidence（厂商、插件名、版本、effect/instrument/hybrid 类型与 tags）、预期 raw probe status、非静音/静音、note response、note-to-audio frame/microsecond latency、non-finite、clipped、RMS/peak 阈值、最小参数数、最小 automatable 参数数、component/controller state 字节数与 state roundtrip 成功状态（可通过 `stateRoundtripProbe: false` 跳过主动 state roundtrip）、controller component-state 初始化同步成功状态、unit/program 数量、program/unit data 支持数、component handler 可用性、可通过 `controllerEditProbe: false` 跳过主动 edit-probe、指定 `controllerEditProbeParameterId` 的主动 edit-probe 成功和事件数、component/controller connection-point 连接状态与 notify-probe 成功状态（可通过 `connectionNotifyProbe: false` 跳过主动 notify-probe）、unit info、program list data、unit data、选中 input/output audio bus index/channel、zero-input instrument 无 input bus、输出 MIDI/event 与 output advanced event data/note-expression/chord/scale 分类型计数、advanced payload 事件数/字节数/raw/text 计数及 truncated/unavailable/invalid-text 上限、输出 parameter-change 规范化/过滤计数、processTimeMicros p50/p95/p99/max 上限、latencySamples/tailSamples 上限、processContextRequirements 精确/bitmask 断言以及预期 compatibility/classification category，第三方 evidence 缺厂商/插件名/类型时会被 manifest 校验拒绝，`minComponentHandlerEvents: 0` 这类无效门禁会被 manifest 校验拒绝，`timeoutMillis: 0` 会被 manifest 校验拒绝，顶层 `evidenceRequirements` 可强制所有 case 具备 evidence、第三方 effect/instrument/hybrid 最小覆盖数和必需 tags，未满足 expectations 时会以 `expectation-failed` 标记矩阵失败，预期的负向 VST3 兼容失败也能保留 raw `probeStatus` 后作为 case 通过；`runtime-probe` 失败也会输出 schema version 1 JSON failure report，matrix result 会保留 `probeReport` 与 case evidence 便于真实插件兼容问题聚合，后续 Bridge/Web/真实插件稳定性测试可复用。
- Runtime matrix 可通过 `requireTailInfo` 要求 `runtime-probe` 报告显式包含 `tailInfo.kind` 和 `tailInfo.samples`；`expectedTailKind` 和 tail kind summary 只读取显式 `tailInfo.kind`，不再从 `tailSamples` 推断。
- Runtime matrix 顶层 `evidenceRequirements` 已支持 `requiredTagCounts`、`requiredThirdPartyTagCounts`、`requireThirdPartyExpectations`、`minThirdPartyBlocks`、`minThirdPartyProcessFrames`、`minThirdPartyTimeoutMillis` 和第三方 expectation quality gates；固定第三方 VST3 清单可以直接要求至少 N 个 `controller-rich`、`note-response`、`output-events` 等 tag 覆盖，并拒绝过短、无 case-level expectations、timeout 过低，或缺少 audio health、process timing、runtime tail/latency、note-response、controller-rich、output-event 关键断言的第三方 smoke case，避免只满足 effect/instrument 数量却没有覆盖关键能力。
- Runtime matrix `controllerHealth` 已进一步汇总 controller component-state sync 的 attempted/success/failed case 数、同步 state 字节总量和最大 case；case-level expectations 可用 `requireControllerComponentStateSync` 与 `minControllerComponentStateSyncBytes` 同时约束真实插件 controller 是否收到足够的 component state。
- `wvst-vst3-host` 已提供可选 `IUnitInfo` facade，能读取 units、program lists、program names 和 selected unit；`wvst-host-worker` / Bridge / Web SDK 已提供 `instance.units` / `client.instances.units()` 查询 API。
- Web/Bridge/worker/VST3 facade 已提供 UI 发起的参数 edit gesture API：`instance.parameter.beginEdit`、`instance.parameter.performEdit`、`instance.parameter.endEdit`；`performEdit` 会调用 controller `setParamNormalized`，三类 gesture 会写入 component-handler event snapshot，且 VST3 host facade 已校验 `begin -> perform* -> end` 配对顺序，避免重复 begin 或无 begin 的 perform/end 污染参数事件流。
- Web/Bridge/worker/VST3 facade 已提供 component/controller connection-point notify API：`instance.connection.notifyComponent`、`instance.connection.notifyController` / `client.instances.notifyComponent()`、`client.instances.notifyController()`，Web 可传入 `messageId` 和 int/float/string/binary typed attributes，由 worker 构造 host-owned `IMessage` 后调用对应 VST3 connection point。
- `wvst-vst3-host` 已提供可选 `IMidiMapping` facade；`wvst-host-worker` 会在 VST3 runtime 初始化后缓存 channel/controller 到 ParamID 的映射，并将 MIDI CC、pitch bend 和 channel aftertouch 转换为 VST3 parameter changes 随当前 audio block 输入。
- VST3 runtime process path 已将插件写回的 output note on/off、poly pressure、legacy MIDI CC out、data、note-expression value/text/int-value、chord、scale 和 output parameter changes 规范化为 WVST 协议事件，并由 worker audio IPC 在响应 frame 中编码为 audio + MIDI event section + parameter automation section + VST3 advanced output event section；advanced section 使用固定 104 字节槽位，前 40 字节表达 data size/type、note-expression typeId/noteId/value/textLen/intValue、chord root/bass/mask/textLen 和 scale root/mask/textLen，后 64 字节在 VST3 `addEvent()` 边界复制 raw `DataEvent.bytes` 预览或 UTF-8 note-expression/chord/scale text，并通过 truncated/unavailable/invalid-text flags 暴露 payload 状态；未知、越界或 payload 固定字段非法的 VST3 output event 会被过滤以避免污染 Web 数据面，同时最近一次 process 的 output event / output parameter-change raw、normalized、filtered、advanced 分类型计数和 advanced payload 健康计数已进入 worker runtime diagnostics 和 runtime matrix summary，runtime-probe JSON 会保留有界 advanced event 样本和 payload 预览，便于真实插件兼容测试定位丢失原因。
- Workspace 已新增 `wvst-embed` crate，提供可嵌入 `BridgeRuntime` / `BridgeHandle`，支持应用内启动 Bridge Server、读取绑定地址、主动 shutdown、runtime event subscription、最近事件快照、外部 worker executable 注入、worker timeout 配置，以及 token、origin allowlist、loopback origin、worker auto-restart 和 control message 大小等 Bridge policy 配置。
- `wvst-embed` 的 `BridgeHandle` 已提供只读 metrics snapshot 和 runtime diagnostics 聚合，嵌入式宿主可直接读取本地地址、指标和 recent events 做健康检查/日志集成；`BridgeRuntimeLogRecord` 已提供 event/diagnostics JSON-lines 写入 helper，`crates/wvst-embed/examples/embedded_bridge.rs` 提供可编译的应用生命周期和结构化日志管线集成示例，覆盖配置、事件订阅、启动、diagnostics 读取和优雅 shutdown；`crates/wvst-embed/examples/packaged_desktop_host.rs` 提供随桌面 App 分发 host worker 并在进程内启动 Bridge 的示例，覆盖 worker 路径发现、origin/token policy、资源限制和 diagnostics 输出。
- Workspace 已新增 `wvst-process-supervision` crate，将 worker 进程树终止的 Unix process group 与 Windows Job Object 平台 FFI 收敛到独立安全 API；`wvst-bridge-server` 继续保持 `unsafe_code = deny`。
- Bridge worker supervisor 已接入可选 worker address-space memory cap 和 CPU time hard cap：`WVST_WORKER_MEMORY_LIMIT_BYTES` / `BridgeConfig::with_worker_memory_limit_bytes()` 与 `WVST_WORKER_CPU_TIME_LIMIT_SECONDS` / `BridgeConfig::with_worker_cpu_time_limit_seconds()` 会通过 `wvst-process-supervision::WorkerResourceLimits` 传给 worker spawn；Unix/macOS/Linux 在 child `exec` 前设置 `RLIMIT_AS` / `RLIMIT_CPU`，Windows Job Object 会设置 job memory 和 job user-time limit。Linux 还新增 cgroup v2 backend，可通过 `WVST_WORKER_LINUX_CGROUP_PARENT`、`WVST_WORKER_LINUX_CGROUP_MEMORY_MAX_BYTES`、`WVST_WORKER_LINUX_CGROUP_CPU_QUOTA_MICROS` 和 `WVST_WORKER_LINUX_CGROUP_CPU_PERIOD_MICROS` 为每个 worker 创建独立 cgroup 并写入 `memory.max` / `cpu.max` / `cgroup.procs`；cgroup 配置失败会以 `supervision-setup-failed` 结构化错误返回，Bridge Server 自身仍不含 unsafe。
- `wvst-bridge-server diagnose` 已提供本地 JSON 诊断命令，可输出 bridge version、平台信息、当前 env-derived config、token 是否启用但不泄露 token 值、host worker 路径/存在性/timeout 和 WVST 环境变量状态，便于 macOS 安装、启动和嵌入式宿主集成排查。
- Workspace 已新增 Rust-first `wvst-packager` crate，`wvst-packager macos` 可生成包含 bridge/host-worker 二进制、launcher、LaunchAgent plist、env 示例、安装脚本、卸载脚本、诊断脚本、copy-truncate 日志轮转脚本、bundle verify 脚本、Developer ID codesign/notarytool 公证脚本和 README 的 macOS release bundle；`wvst-packager linux` 可生成包含 bridge/host-worker 二进制、launcher、systemd user service、env 示例、安装脚本、卸载脚本、诊断脚本、user journal vacuum 维护脚本、bundle verify 脚本和 README 的 Linux release bundle；`wvst-packager windows` 可生成包含 bridge/host-worker `.exe`、PowerShell launcher、env 示例、Scheduled Task 安装/卸载脚本、诊断脚本、copy-truncate 日志轮转脚本、bundle verify 脚本和 README 的 Windows release bundle；三端 bundle 均会写入 schema version 1 的 `wvst-package-manifest.json` 作为机器可读包结构 evidence，verify 脚本会写入 schema version 1 的 `wvst-verify-report.json`（可用 `WVST_VERIFY_REPORT` 覆盖路径）记录 status、checksRun、checksFailed、checksSkipped、warnings、strictSignature 和实际覆盖的 checks 列表；安装脚本会生成本机 token、安装用户级启动项、安装日志维护入口并保留配置以便排障，诊断脚本会加载已安装配置、设置 host worker 路径、报告用户级启动项状态、汇总日志位置/最近日志，并执行 `wvst-bridge-server diagnose`；verify 脚本可在不安装服务的情况下检查包结构、服务定义/脚本关键内容、本地 `diagnose` 入口，并在 macOS/Windows 上支持严格签名状态检查开关。当前已用本机 debug bridge/worker 对 macOS、Linux 和 PowerShell Windows bundle 做生成与 verify-report 落盘 smoke，其中 macOS 非严格模式只记录未签名 Gatekeeper warning，Linux 在 macOS 上跳过 `systemd-analyze`，Windows 在 macOS PowerShell 上跳过 Authenticode cmdlet；仍需真实目标 OS 安装与签名证据。
- Workspace 已新增 `wvst-shm-transport` crate，提供共享内存音频 transport 的 Rust-first layout/descriptor 抽象：固定 header 与 input/output ring descriptor、cache-line 对齐、cursor/audio region offset 计算、zero-input instrument 布局、容量/通道/溢出校验、基于单调 read/write frame cursor 的 readable/writable/backpressure 计算、wrap-around span plan、span byte range 提取、descriptor/cursor/audio region range 暴露、共享内存总长度/range 边界校验、从完整 memory bytes 解析并校验 layout、64 字节 cursor block ABI（read/write frame、drop/underflow/overflow counters、generation 和 flags），以及可供 Web WASM、Bridge 和 host-worker 共识的 little-endian descriptor/cursor 字节编码、反解析和 canonical layout 校验；该 crate 已提供 memory slice 级初始化和 interleaved f32 ring I/O，可推进 cursor、处理 wrap-around、支持 zero-input instrument frame，并在读/写背压失败时记录 underrun/overrun counters。Workspace 也已新增 `wvst-shm-mmap` crate，提供 file-backed mmap 创建/打开、descriptor 初始化、跨 mapping ring I/O smoke，以及 mmap cursor 字段的 Rust atomic load/store/counter 更新视图；Bridge 控制面已提供 `stream.sharedMemory.create` / `stream.sharedMemory.destroy` / `stream.sharedMemory.status` / `stream.sharedMemory.process`，可为实例 stream 分配 file-backed mmap transport、自动让 host-worker attach/detach、返回 schema version 1 descriptor/layout/path，并暴露 input/output ring cursor、readable/writable frames 和 backing file 存在性诊断，在 stream close 或 instance destroy 时清理资源；host-worker 已能打开 Bridge 创建的 mmap，校验 layout 与实例 sample-rate/block/channels 一致，有输入时从 input ring 读取一个 block，zero-input 音源实例会跳过不存在的 input ring 消费，经现有 backend `process_interleaved_f32` 处理后写回 output ring；`stream.sharedMemory.process` 已支持随单个 block 传入 `midiEvents` 和 `parameterEvents`，在隔离 worker 内复用二进制音频 IPC 的 VST3 note/poly pressure、MIDI mapping 与 sample-accurate parameter-change 转换逻辑，并在结果里报告 `inputEvents` 计数；input underrun / output backpressure / layout / event validation / backend process 错误都会返回结构化 `reason` 和诊断数据；Bridge 控制面已提供 `stream.sharedMemory.pump.start` / `stop` / `status` 持续共享内存处理调度，可按 block duration 或指定 intervalMicros 周期调用隔离 worker 的 mmap process path，也可启用 adaptive 调度，按 input/output ring backlog 做 input underrun / output backpressure preflight skip、catch-up delay 与 idle backoff，并把成功/失败、preflight skip、input underrun、output backpressure、worker error、overrun、最近 ring status、当前调度延迟、last outcome 和 tick-based MIDI/parameter event queue 状态纳入 per-pump status 与全局 shared-memory process metrics；`stream.sharedMemory.pump.enqueueEvents` 可按 targetIteration 或 delayIterations 为后续 pump tick 排队 MIDI/parameter events，支持 `maxQueuedEvents`、reject/drop-oldest overflow policy、late/drained/dropped/cleared 计数；`stream.sharedMemory.pump.clearEvents` 可清空待发事件，便于 UI 停止音符、切换 patch 或断开 graph；Bridge metrics 和 Web SDK `BridgeMetrics` 已暴露 pump event queue enqueued/drained/late/dropped/cleared 全局计数，`wvst-stability-budget --bridge` 可直接读取原始 Bridge metrics/pump status JSON 并把这些指标纳入压测预算；`pump.stop` 返回的状态快照会明确 `running=false`，随后 `pump.status` 不再暴露已移除的运行记录；Web SDK 已提供 shared-memory descriptor/status/process/pump/event-queue 的 typed control facade，新增 `createWVSTSharedMemoryPumpSession()` 高层 helper 封装 create/start/enqueue/clear/status/process/stop/destroy 生命周期，并提供 `createWVSTSharedAudioTransportView()` Web 侧 SharedArrayBuffer/ArrayBuffer binding，可按 Rust layout 绑定 cursor/audio region、读写 cursor、计算 readable/writable frames、处理 wrap-around interleaved f32 ring I/O，供 UI/诊断和未来 Web 端数据面接入复用；stream close 或 instance destroy 会自动停止 pump。暂不包含浏览器到本地 Bridge 的真实零拷贝共享内存接入或真实负载下的调度/背压参数校准。

## 距离完整能力的主要差距

### 1. 插件实例运行态生命周期

仍缺少：

- `ready` 之后的 `processing` / `stopped` 生命周期已有控制 API，`starting`、`stopping`、自动恢复中等瞬态状态和事件推送已有首版；WebSocket server-push notification 已能把 Bridge event 主动发给授权 Web 客户端，start/stop/destroy 已补充更细粒度的 `worker-processing-starting`、`worker-processing-stopping`、`worker-destroying` 和 `worker-destroyed` 事件，stream open/close 已补充 `stream-opened`、`stream-closing` 和 `stream-closed` 事件；应用级 `worker-policy-decision` 事件已有首版，覆盖 quarantine release/reject、worker 实例资源上限拒绝、同插件 idle worker 保守回收、手动 restart、heartbeat 自动 restart 和自动恢复禁用拒绝，仍缺少更完整的调度决策与真实压测校准。
- 每个实例的独立 worker 进程已具备原型，并支持手动 restart 与 heartbeat 驱动的自动 restart；崩溃/恢复/quarantine 事件、worker 实例数量上限、同插件 closed idle worker 保守回收、memory hard cap、CPU time hard cap 和 Linux cgroup v2 CPU/memory quota 已有首版，仍缺少真实 Linux cgroup 部署验证、基于压测的资源策略校准和池化调度策略。
- 同一插件 N 个实例的 worker 池化、调度和资源上限策略；当前更接近一实例一 worker 的保守隔离原型。

### 2. 持久 worker IPC

当前 Bridge 已能为实例启动并绑定一个常驻 worker 进程，控制面和音频面都已有独立 framed IPC；JSON-line 控制模式已删除，测试 fixture 也走 framed IPC。

仍缺少：

- 更完整的 Bridge worker supervisor 生命周期管理已有恢复中状态、事件快照、WebSocket server-push 事件订阅、quarantine 解除策略、worker kill/wait 审计指标、Unix/macOS 进程组终止、Windows Job Object 终止、Unix `RLIMIT_AS` / `RLIMIT_CPU` worker hard cap、Windows Job Object memory/user-time hard cap、Linux cgroup v2 CPU/memory quota 和 framed control IPC 首版；仍缺少 Windows 真实运行验证、Linux cgroup 真实部署验证和更多失败分类。
- framed control IPC 已替换生产路径 JSON-line 控制面，并已有 schema version、max body、sequence id、status code、error response frame capability 校验、header-level error classification、sequence/status 校验、body size cap、wire-level 批处理/多路复用 frame、`instance.metadata.refresh` 的 Bridge 批量 metadata refresh 入口、`instance.runtime.snapshot` 的实例挂载聚合入口、`instance.parameter.edit` 参数 edit gesture 聚合入口、`instance.state.setAndRefresh` 状态写入后刷新入口、unit/program data 写入后刷新入口，以及 connection notify 后刷新入口；仍缺少基于真实插件 UI/message 交互数据继续识别更多高频组合调用。
- 超时后的全链路 kill/wait 审计已有首版 counters，且 Unix/macOS 已覆盖进程树维度；quarantine 已暴露释放倒计时诊断、可配置失败阈值，restart/recovery 事件已暴露手动/自动模式、触发原因、恢复失败原因和结构化错误，仍缺少基于真实插件压测数据的默认策略校准。
- worker hello 已有基础 capability negotiation 和 framed control IPC schema/body/capability 校验，实例级 `runtimeCapabilities` 已能按数据面、MIDI、参数自动化和诊断能力暴露 schema v2 capability reason/hint；worker rejection 已能透传 VST3 runtime init、control 和 `process()` 阶段化失败 data，framed IPC 已能在 header 层标记 worker rejection，并已提供批处理/多路复用 frame、Bridge 层批量 metadata refresh 入口、首版 compatibility category/hint，以及 host-worker one-shot / persistent worker supervisor / IPC / quarantine / resource-policy 错误的 schema version 1 classification/hint；仍缺少真实插件错误样本校准。

### 3. 真实 VST3 component/controller lifecycle

当前已完成 factory info 读取、`IPluginFactory::createInstance` component probe、`IAudioProcessor` 接口探测、基础 ABI skeleton、纯 Rust lifecycle 状态机、processor facade、component holder、macOS factory 到 holder 的创建路径，以及 worker runtime backend 接入。

仍缺少：

- 更完整的多 bus arrangement 和 process buffer 映射；当前 holder 已提供基础 `IHostApplication`、host-created `IMessage` / `IAttributeList`、audio bus 查询、selected-bus activation、selected audio bus diagnostics，并支持单个主 bus 的 mono/stereo/常见 3.0 到 7.1 speaker arrangement。
- `IEditController`、`IComponentHandler`/`IComponentHandler2` callback 事件记录与 Bridge server-push、`IConnectionPoint` connect/disconnect/notify、参数列表、unit/program metadata、normalized 参数读写、带顺序校验的 UI 参数 edit gesture、normalized/plain/display string 转换、component/controller state get/set 聚合、unit selection、unit-by-bus、program/unit data 读写以及 parameter-change queue/sample-accurate automation 已有首版；metadata invalidation 已能区分 metadata refresh、audio graph rebuild 和 component reload policy；仍缺少真实第三方 controller/automation/unit-info/program-data/message notify/connection-point 兼容验证，以及真实 host latency/bus 变化后的应用层重建测量。
- 真实第三方插件兼容验证仍不足；当前 controller 参数枚举、state 读取、unit/program metadata、program/unit data 支持、handler edit-probe、connection point、`setProcessing`、`process`、process context、latency/tail、selected audio bus、output audio health、output event/parameter diagnostics 和 zero-input instrument note timing 主要由 fake ABI fixture、Bridge runtime-info 传播测试、可对真实 bundle 执行的多 block `runtime-probe` smoke 入口和 `wvst-testkit` runtime matrix runner 覆盖。本机 macOS 已用 Native Instruments Reaktor 6.vst3 跑通真实第三方 effect + instrument matrix：`component-probe` 可创建 `IComponent` 并确认 `IAudioProcessor`，FX 2-in/2-out 和 instrument zero-input + MIDI note case 均完成 setup/activate/process/stop/terminate，component state 11 bytes roundtrip 成功，selected main stereo bus index 0，报告 `passed=2`、`timedOut=0`、non-finite/clipped 为 0，并确认 Reaktor raw `tailSamples=4294967295` 会在 worker probe 与 matrix report 中语义化为 `tailInfo.kind=infinite`；该插件默认无 ensemble 因而输出为全静音，且没有独立 controller class id，所以还不能证明 controller 参数、component handler、connection-point、unit/program 和非静音 note response 的真实第三方覆盖。matrix manifest/report 已能记录第三方插件 evidence 并汇总 effect/instrument/hybrid、vendor、tag、selected audio bus coverage、component handler edit-probe coverage 和 process timing 最慢 case，同时汇总全静音、non-zero、non-finite、clipped、peak/RMS、note response 缺失、finite/infinite tail 分布和最慢 note-to-audio timing 异常线索，并可按 case 断言选中的 VST3 input/output bus、输出事件/advanced 分类型/advanced payload 健康/参数变化健康、processTimeMicros p50/p95/p99/max 上限、latencySamples、tailSamples、`expectedTailKind` 和 process context requirements；manifest 顶层 `evidenceRequirements` 已能把第三方 effect/instrument/hybrid 覆盖不足和第三方 case expectation 质量不足作为配置错误提前拒绝，仍需要在 CI/本地纳入固定第三方插件清单与长时稳定性记录。

### 4. 低延迟音频数据面

当前 Bridge 二进制帧已具备按 `streamId` 到 worker 二进制 audio IPC 的 VST3 runtime 路由，但仍不是最终低延迟数据面。

仍缺少：

- stream open/close 已有首版控制 API、end-of-stream 诊断帧和 close 后 in-flight drain；WebAudio session 已提供基于 `transportFailures` 的主线程自动重开策略，仍缺少真实浏览器/设备负载下的默认参数校准。
- Web Worker 从 SAB 取音频块并编码发送已有基础 ring-buffer audio pump、主动丢弃过期 quantum 和 late event 计数，并已预分配 input scratch 与 silence output buffer，避免 transport failure 诊断静音路径反复分配；仍缺少更完整的延迟配置、调度调优和真实负载校准。
- Web 设备选择已有底层 helper、高层 session graph helper、capability API、device watcher、sample-rate guard、手动 stream restart、自动 stream restart、sample-rate mismatch `rebuildRequired` 诊断和扩展 loopback/session metrics；仍缺少真实端到端设备切换测量，以及应用层按新 sample rate 重建实例/graph 的完整示例。
- Bridge 到 worker 的二进制 audio IPC 已具备首版；Bridge 侧 audio IPC 请求 frame buffer 已随 worker audio connection 复用，且路由热路径会直接借用 WebSocket binary payload 编码 worker request，避免每个 audio block 额外复制输入帧；Bridge/Web 二进制诊断帧已有基础 flags，每 stream backpressure drop 会返回 `silence` / `late` 诊断帧并计数；共享内存 audio ring 的 layout/descriptor、cursor/span planning、memory slice 级 interleaved f32 ring I/O、Web SharedArrayBuffer/ArrayBuffer view binding、file-backed mmap、mmap atomic cursor、Bridge stream shared-memory resource lifecycle、worker attach/detach、status diagnostics、process-once mmap 音频处理、zero-input shared-memory process、process-once MIDI/parameter event 注入、显式启动/停止的持续 pump、adaptive preflight/backoff/catch-up 调度、tick-based pump MIDI/parameter event queue、shared-memory process metrics、pump preflight skip/overrun 计数、pump event queue 全局计数与 stability budget gating、最近/最大处理耗时、最近 overrun 余量、最近 ring status、input underrun / output backpressure / worker error 分类计数和 last outcome 诊断已具备首版，仍缺少浏览器到本地 Bridge 的真实零拷贝共享内存接入、Web/Bridge/worker 端到端背压协调以及真实负载下的 pump interval/调度策略校准。
- worker 路径已验证 sample rate / max block / processing state，并预分配输入/输出 sample scratch buffers、复用请求/响应 body buffer；runtime backend 已接入真实 VST `process()`，且 VST3 host 已捕获插件写回的 output events/parameter changes 并编码回 Web 响应帧，最近一次 process 的 output 过滤统计也会进入 runtime diagnostics；VST3 `LegacyMIDICCOutEvent` 已规范化为 WVST structured MIDI 或 raw MIDI 输出，data、note-expression、chord 和 scale 等高级 VST3 output event 已通过 audio frame 第四段固定长度 binary section 返回 Web，并进入 runtime matrix advanced 分类型和 payload 健康 summary；Data/text payload 现在会以 64 字节固定槽做边界复制和 Web 解码预览。当前仍经 worker instance mutex 串行处理、保留 interleaved/planar scratch copy，且高级 VST3 output event 的 Web 应用层消费示例仍待扩展。
- late/drop/jitter/backpressure 首版 Bridge 诊断指标已完成，Web SDK 已把 WebAudio dropped quantum、late/dropped MIDI/parameter event、transport failure、session auto restart/rebuild-required 纳入 session metrics，`wvst-testkit` 已提供可复用 latency harness、scripted latency snapshot CLI、30 分钟 stability runner 和可序列化 budget evaluator，并可把 WebAudio underflow/overflow/pending/dropped/late/transport、WebAudio end-to-end round-trip p95/p99、Bridge shared-memory process latency p95/p99 与 pump last/max process/overrun timing 放进同一预算报告；Bridge 侧预算输入已兼容原始 `bridge.metrics`、pump status 和 runtime snapshot JSON；仍缺少真实浏览器采集器落盘 WebAudio end-to-end 往返测量、共享内存数据面自动接入和真实负载校准。

### 5. MIDI 与音源 VST

仍缺少：

- MIDI/note event schema、Web/Bridge/worker 二进制数据面传输、shared-memory process-once 事件注入、VST3 `IEventList` note/poly pressure 转换，以及 CC、pitch bend、channel aftertouch 经 `IMidiMapping` 到 VST3 parameter-change path 的映射已有首版。
- Rust worker 边界已具备 block 内 sample offset 确定性排序；Web worker 侧已加入目标 quantum 绑定、late-event 丢弃和计数策略；Web SDK 已提供 Web MIDI adapter、Web MIDI message decoder、虚拟键盘 helper 和默认键盘映射；仍缺少真实 Web MIDI / 虚拟键盘端到端调度校准。
- 音源 VST 的 zero-input audio buffer/session/worker plumbing 已有首版；note on/off 已能随 block 进入真实 VST3 `process()`，`runtime-probe` 已能记录 note-on/note-off 绝对 frame、首个非零输出 frame 和 note-to-audio latency，并可通过 matrix expectations 约束 `requireNoteResponse`、`maxNoteToAudioFrames` 和 `maxNoteToAudioMicros`；仍缺少真实第三方 instrument 兼容测试和浏览器端 Web MIDI / 虚拟键盘示例验证。

### 6. 嵌入式 runtime 与打包

仍缺少：

- `wvst-embed` 已有 runtime builder、事件订阅、事件快照、只读 metrics diagnostics、外部 worker executable 注入、timeout 配置、auth/origin/control-message policy 配置、worker resource limit 配置与清除方法、JSON-lines 日志 helper、可编译应用生命周期/日志管线示例和随桌面 App 分发 host worker 的嵌入式启动示例；仍缺少真实宿主 App bundle 安装、升级、签名和故障恢复流程验证。
- macOS 安装、启动、授权和日志已有基础诊断命令支持，`wvst-packager macos` 已提供首版发布 bundle 生成、安装、卸载、诊断、copy-truncate 日志轮转、bundle verify、机器可读 package manifest / verify report，以及凭 `WVST_CODESIGN_IDENTITY` / `WVST_NOTARY_PROFILE` 或 Apple ID 凭据运行的签名/公证脚本；Linux 已提供 systemd user service 发布 bundle 生成、安装、卸载、诊断、user journal vacuum 维护脚本、bundle verify 和机器可读 package manifest / verify report；Windows 已提供 Scheduled Task 发布 bundle 生成、安装、卸载、诊断、文件日志写入、copy-truncate 日志轮转、bundle verify 和机器可读 package manifest / verify report；`wvst-testkit` 已提供 `wvst-package-evidence` 对这些 manifest/report 做 CI gate，并能强制平台服务类型、runtime 文件声明、verify script、manifest/report checks 一致性和必需 verification checks；仍缺少 macOS 真实 Developer ID 签名/公证运行记录和三平台真实安装验证记录。
- Windows 真实运行验证和 Linux cgroup 资源限制真实部署验证。

## 建议下一阶段

1. 扩展固定第三方 VST3 runtime matrix 清单：在 Reaktor 6 的静音默认 preset 之外，加入至少一个 controller-rich effect、一个会对 MIDI note 产生非静音输出的 instrument，以及一个带参数自动化/输出事件的插件。
2. 用真实第三方插件继续验证 controller/automation/unit-info/program-data/message notify/typed attribute 兼容性，并把已知失败用 `expectedStatus`/classification expectations 固化。
3. 将 framed control IPC 的批处理/多路复用能力继续扩展到更多 Bridge metadata/control 组合调用，并继续扩展错误分类。
4. 增加 Windows/Linux 真实运行验证、worker supervision 压测、资源上限策略调优和更细粒度 server-push 事件类型。
5. 继续把 Bridge audio sequence/late/jitter、shared-memory pump event queue 指标与 WebAudio worker/worklet underflow/overflow 指标纳入同一真实压测报告，并把 Bridge route latency 扩展到端到端 WebAudio 往返测量。
