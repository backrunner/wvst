[package]
name = "wvst-scanner"
description = "VST3 bundle discovery and metadata parsing for WVST."
authors.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"

[lints]
workspace = true

