---
title: Live Demo
description: 通过浏览器侧 WVST effect rack 播放并处理本地音频文件。
order: 4
---

# Live Demo

<WVSTRackDemo locale="zh" />

## 覆盖的真实链路

这个 live demo 使用 WVST 应用会用到的公开导出：

- `WVSTClient.connect()`：`bridge.hello`、token/origin 协商、plugin APIs、instance APIs、metrics 和 events。
- `WVSTBridgeWorkerClient`：DedicatedWorker transport。
- `createLoopbackSharedBuffers()`：浏览器 SharedArrayBuffer rings。
- `configureLoopbackAudioWorkletNode()`：手动配置 `wvst-loopback` nodes。
- `readLoopbackMetrics()`：rack slot counters。
- `@wvst/web/bridge-worker?worker` 与 `@wvst/web/loopback-processor?url`：Vite worker/worklet bundling。

demo 不模拟插件处理。低延迟浏览器前置条件、Bridge、plugin scan、VST3 loading、worker startup 或 audio stream 任一环节失败时，界面都会显示错误状态。

## 前置条件

- 文档页从 localhost 或 HTTPS 提供。
- 发送 `Cross-Origin-Opener-Policy: same-origin`。
- 发送 `Cross-Origin-Embedder-Policy: require-corp`。
- `wvst-bridge-server` 在 demo 显示的 endpoint 上运行。
- 第一版 rack workflow 使用能处理 stereo input/output 的 VST3 effect class。
- 修改 SDK 源码后，运行文档 app 前先构建 `@wvst/web`。

## Rack 行为

每个 rack slot 拥有：

- `client.instances.create()` 返回的 WVST instance descriptor。
- `client.instances.start()` 启动的 processing lifecycle。
- 一个 `LoopbackSharedBuffers` allocation。
- 一个名为 `wvst-loopback` 的 `AudioWorkletNode`。
- 一个以 WVST `streamId` 为 key 的 Bridge worker audio stream。
- 从 shared counter buffer 读取的 metrics snapshot。

挂载、bypass、删除或移动 slot 时，WebAudio graph 会重建：

```txt
media element source -> slot 1 -> slot 2 -> output gain -> analyser -> destination
```

bypassed 或 removing slot 会从 WebAudio graph 中跳过，但 cleanup 完成前仍会显示在列表里。

## 插件选择

demo 连接后调用 `client.plugins.list({ rescan: false })`。rescan 按钮调用 `client.plugins.scan()`。

插件选项来自 `PluginDescriptor.classes`。看起来像 effect 的 class 会排在 instrument 前面，但第一版仍然保守：它假设是 stereo effect rack，不展示 instrument UI 或 generic parameter panel。

## AudioWorklet 加载

demo 在每个 `AudioContext` 中只加载一次 AudioWorklet module：

```ts
workletModule ??= context.audioWorklet.addModule(loopbackProcessorUrl);
await workletModule;
```

新的 rack slot 会创建自己的 `AudioWorkletNode`，并配置新的 buffers。不要在同一个 `AudioContext` 中反复加载同一个 worklet module；浏览器会拒绝重复的 `registerProcessor("wvst-loopback")`。

## Cleanup 规则

slot 删除和组件销毁时，数据路径按这个顺序停止：

1. 断开 AudioWorklet node。
2. 停止 Bridge worker audio stream。
3. 停止 instance processing。
4. 关闭 stream。
5. 销毁 instance。
6. 重建剩余 WebAudio graph。

文件切换会 revoke 上一个 object URL。完整 shutdown 会关闭 Bridge client、worker client、worker、audio graph 和 `AudioContext`。

## 验收清单

修改 demo 后按这个检查：

- 打开 `/docs/live-demo` 和 `/docs/zh/live-demo`。
- 确认 light/dark theme 下 rack 都足够紧凑。
- 确认 hidden file input 没有显示出来。
- 确认 `Endpoint`、`Token`、`Effect VST` 和 action buttons 使用 Svedocs form components。
- 不启动 Bridge 时点击 connect，确认显示明确失败，而不是 mock playback。
- 启动 Bridge 并准备一个已知 2-in/2-out VST3 effect，挂载一个 slot 后确认有音频返回。
- 挂载两个 slots，确认 WebAudio graph 仍然是串联。
- 播放时切换 bypass 和移动 slot。
- 删除 slot，确认 instance/stream 被销毁。
- 观察 slot metrics 中的 underflow、overflow、pending quanta 和 transport failures。
