# WVST Docs

SvelteKit documentation site for WVST, generated from the local `../svedocs` framework.

```sh
npm run docs:dev
npm run docs:check
npm run docs:build -- --no-og
```

The live rack demo uses real WVST bridge processing. Run `cargo run -p wvst-bridge-server` before connecting from the docs page.
